/** Minimum trimmed length for process description text (shared by HTTP layer and analyzer). */
export const MIN_PROCESS_TEXT_CHARS = 5;

/** Maximum trimmed length to limit memory, model cost, and abuse (shared by HTTP layer and analyzer). */
export const MAX_PROCESS_TEXT_CHARS = 80_000;
